//Produce a result in the display using the number buttons and number/or result and operator

function pushBtn(equal){
  document.calculator.display.value = document.calculator.display.value + equal
}

