//Display the result of number equation in the display

function pushBtn(equal){ //the object-function has a property with the name "pushBtn" and a parameter of equal 
   document.calculator.display.value = document.calculator.display.value + equal //the code will look through the documents for a line with the name calculator and then find another line under calculator entitled display and that value  will equal the display value + another value. The equal value will  evaluate the two or more numbers displaying on the screen
}

